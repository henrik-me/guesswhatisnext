name: CI/CD Pipeline (Disabled)

# This workflow is intentionally disabled.
# Deploy pipelines are being reworked in Phase 10:
#   - staging-deploy.yml (hourly cron) — planned in task 54
#   - prod-deploy.yml (manual trigger) — planned in task 55
#
# Push to main no longer triggers any deployment.

on:
  workflow_dispatch: {}

jobs:
  notice:
    name: Pipeline disabled
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploy pipelines moved to staging-deploy.yml (task 54) and prod-deploy.yml (task 55)"
