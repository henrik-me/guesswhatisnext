receivers:
  otlp:
    protocols:
      http:
        endpoint: 0.0.0.0:4318
exporters:
  file:
    path: /data/traces.json
service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [file]
